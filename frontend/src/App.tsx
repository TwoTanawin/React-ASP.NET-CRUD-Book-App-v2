import React from 'react';
import BooksApp from './component/BooksApp'; // Assuming you have a `BooksApp.tsx` file for the component

const App: React.FC = () => {
  return (
    <div>
      <BooksApp />
    </div>
  );
};

export default App;
